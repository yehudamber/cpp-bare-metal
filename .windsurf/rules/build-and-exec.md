---
trigger: always_on
---

# Build and execute

Whenever making changes to the project:
- Configure the program using `cmake --preset <preset-name>`
- Build the program using `cmake --build --preset <preset-name>`
- Execute the program using `./exec.sh <preset-name>`
- Repeat the above steps for all presets
